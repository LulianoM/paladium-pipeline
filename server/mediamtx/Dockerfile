# Começa com a imagem oficial do MediaMTX
FROM bluenviron/mediamtx:latest

# Copia nosso arquivo de configuração local para dentro da imagem,
# substituindo o original.
COPY mediamtx.yml /mediamtx.yml